seed = 42          # Random seed for reproducibility
dev_run = false    # Run a small subset of the data for debugging
task = "cls" # cls, reg, tte, mxc (classification, regression, time-to-event, mixture-cure)
simulate = true # Regenerate the tokenized dataset
tune = false       # tune the model hyperparameters
use_best = false   # use the best hyperparameters found during tuning
train = false      # train the model
evaluate = false   # Generate test set model performance metrics 
importance = false  # Compute feature importance
plot = false       # Generate plots/figures
compile = false    # Compile the Torch model
proportions = [0.8, 0.1, 0.1] # proportions for train, val, test splits

[path]
dataset = "data/dataset"                           
hyperparameters = "data/model/hyperparameters.json"
checkpoint = "data/model/checkpoint.pt"                
logs = "data/logs"                                 
metrics = "data/results/metrics.parquet"                   
attribution = "data/results/attribution.parquet"                                      
importance_plot = "data/plots/importance.png"
metrics_plot = "data/plots/metrics.png"

[simulation]
intercept = -2.5   # this affects the prevalence
d_features = 2
scale = 1.0
n_samples = 1000
m_timepoints = 20
variance = 0.5
noise = 0.05
gamma_shape = 1.0
gamma_rate = 1.0

[dataset]
train_size = 0.8
stratify = "label"

[dataloader]
batch_size = 32
num_workers = 0            # > 1 for multiprocessing
pin_memory = true
persistent_workers = false

[model]
embedding_dim = 128
hidden_dim = 256
num_heads = 4
num_layers = 3
dropout = 0.1
compile = false

[loss]
ignore_index = -100
reduction = "mean"

[optimizer]
lr = 1e-4
weight_decay = 1e-6

[scheduler]
gamma = 0.9

[trainer]
max_epochs = 5
gradient_clip = 1.0
device = "auto" # auto, cpu, cuda, mps

[tuner]
n_trials = 16
direction = "minimize"
checkpoint = "checkpoint.pt"

[hparams]
path = "hyperparameters.json"
[hparams.max_epochs]
name = "max_epochs"
low = 2
high = 20
[hparams.hidden_dim]
name = "hidden_dim"
low = 6             # 2^6 = 64
high = 10           # 2^10 = 1024
[hparams.num_heads]
name = "num_heads"
low = 1
high = 3
[hparams.num_layers]
name = "num_layers"
low = 1
high = 6
[hparams.dropout]
name = "dropout"
low = 0.0
high = 0.1
[hparams.lr]
name = "lr"
low = 1e-4
high = 1e-2
log = true
[hparams.weight_decay]
name = "weight_decay"
low = 1e-10
high = 1e-2
log = true

[evaluator]
n_bootstraps = 100

[plots]
style = "darkgrid"
font_scale = 1.5
palette = "viridis"
